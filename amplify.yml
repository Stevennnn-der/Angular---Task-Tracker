version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx ng build task-tracker --configuration production
  artifacts:
    baseDirectory: dist/task-tracker
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
